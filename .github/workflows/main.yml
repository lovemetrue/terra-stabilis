name: Build and Deploy Terra Stabilis

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

env:
  REGISTRY: ghcr.io
  IMAGE_PREFIX: ${{ github.repository }}

jobs:
  initiation:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      - name: Create SSL certificates directory
        run: |
          mkdir -p ~/.cloud-certs
          curl -o ~/.cloud-certs/root.crt "https://st.timeweb.com/cloud-static/ca.crt"
          chmod 0600 ~/.cloud-certs/root.crt

      - name: Install bot dependencies from requirements
        run: |
          cd app/bot
          pip install -r requirements.txt

      - name: Install admin dependencies from requirements
        run: |
          cd app/admin
          pip install -r requirements.txt

  build-and-push:
    needs: initiation
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop'
    permissions:
      contents: read
      packages: write

    strategy:
      matrix:
        component: [bot, admin]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-${{ matrix.component }}
          tags: |
            type=ref,event=branch
            type=sha,prefix={{branch}}-
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push Bot image
        if: matrix.component == 'bot'
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./app/bot/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            BUILDKIT_INLINE_CACHE=1

      - name: Build and push Admin image
        if: matrix.component == 'admin'
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./app/admin/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            BUILDKIT_INLINE_CACHE=1

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Show production files (debug)
        run: |
          echo "Listing production files in app/:"
          ls -la app/ || echo "app/ not found"
          echo "Checking for required files:"
          if [ -f app/docker-compose.yml ]; then
            echo "✓ docker-compose.yml found"
          else
            echo "✗ docker-compose.yml missing"
            exit 1
          fi
          if [ -f app/.env.example ]; then
            echo "✓ .env.example found"
          else
            echo "✗ .env.example missing"
            exit 1
          fi

      - name: Copy production files to server
        uses: appleboy/scp-action@v0.1.4
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          source: "app/docker-compose.yml,app/.env.example"
          target: "/opt/terrastabilis/"
          overwrite: true

      - name: Deploy to production server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            # Создаем директорию если не существует
            sudo mkdir -p /opt/terrastabilis
            sudo chown $USER:$USER /opt/terrastabilis

            # Переходим в директорию проекта
            cd /opt/terrastabilis

            # Если пришёл .env.example — создаём .env из него (при первом деплое)
            if [ -f .env.example ] && [ ! -f .env ]; then
              cp .env.example .env
              echo "⚠️  .env создан из .env.example — обновите значения в .env!"
            fi

            # Создаем директорию для SSL сертификатов
            mkdir -p ~/.cloud-certs
            curl -o ~/.cloud-certs/root.crt "https://st.timeweb.com/cloud-static/ca.crt"
            chmod 0600 ~/.cloud-certs/root.crt

            # Логинимся в GitHub Container Registry
            echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

            # Останавливаем и удаляем старые контейнеры
            docker compose down || true

            # Pull последних образов
            docker compose pull

            # Запускаем контейнеры
            docker compose up -d

            # Очищаем неиспользуемые образы
            docker image prune -f

            # Проверяем статус контейнеров
            echo "📊 Status of running containers:"
            docker ps --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"

            # Проверяем логи бота
            echo "🔍 Bot logs (last 10 lines):"
            docker logs terrastabilis-bot --tail 10 || echo "Bot container not running yet"

            # Проверяем логи админки
            echo "🔍 Admin logs (last 10 lines):"
            docker logs terrastabilis-admin --tail 10 || echo "Admin container not running yet"

            echo "🚀 Деплой успешно завершен!"