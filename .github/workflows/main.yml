name: Build and Deploy Terra Stabilis
on:
  push:
    branches: [ main ]

env:
  REGISTRY: ghcr.io
  IMAGE_PREFIX: lovemetrue/terra-stabilis

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    strategy:
      matrix:
        component: [bot, admin]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-${{ matrix.component }}
          tags: |
            type=ref,event=branch
            type=sha,prefix={{branch}}-
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push Bot image
        if: matrix.component == 'bot'
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./app/bot/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and push Admin image
        if: matrix.component == 'admin'
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./app/admin/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Copy production files to server
        uses: appleboy/scp-action@v0.1.4
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          source: "app/docker-compose.yml,app/.env.example"
          target: "/opt/terrastabilis/"
          overwrite: true

      - name: Deploy and health check
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            set -e

            # Создаем директорию если не существует
            sudo mkdir -p /opt/terrastabilis
            sudo chown $USER:$USER /opt/terrastabilis

            cd /opt/terrastabilis

            # Проверяем наличие файлов в корне
            echo "=== Checking files in /opt/terrastabilis ==="
            ls -la

            # Если файлы в app/, перемещаем их в корень
            if [ -f app/docker-compose.yml ]; then
              echo "📁 Files found in app/, moving to root..."
              mv app/docker-compose.yml ./
              mv app/.env.example ./
              rm -rf app/
              echo "✅ Files moved to root directory"
            fi

            # Проверяем наличие файлов после перемещения
            if [ ! -f docker-compose.yml ]; then
              echo "❌ docker-compose.yml not found after moving!"
              exit 1
            fi

            # Создаем .env из .env.example если нужно
            if [ ! -f .env ] && [ -f .env.example ]; then
              cp .env.example .env
              echo "⚠️  .env created from .env.example - please update sensitive values!"
            elif [ -f .env ]; then
              echo "✅ Using existing .env file"
            else
              echo "❌ Neither .env nor .env.example found!"
              exit 1
            fi

            # SSL сертификаты
            mkdir -p ~/.cloud-certs
            curl -o ~/.cloud-certs/root.crt "https://st.timeweb.com/cloud-static/ca.crt"
            chmod 0600 ~/.cloud-certs/root.crt

            # Логинимся в registry
            echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

            # Деплой
            echo "🛑 Stopping old containers..."
            docker compose down || true

            echo "📥 Pulling latest images..."
            docker compose pull

            echo "🚀 Starting containers..."
            docker compose up -d

            echo "⏳ Waiting for containers to start..."
            sleep 30

            # Проверяем статус контейнеров
            echo "📊 Container status:"
            docker ps --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"

            # Проверяем что все контейнеры запущены
            for container in terrastabilis-bot terrastabilis-admin terrastabilis-redis; do
              if docker ps --format "{{.Names}}" | grep -q "$container"; then
                echo "✅ $container is running"
              else
                echo "❌ $container is NOT running"
                docker logs "$container" --tail 10 || true
                exit 1
              fi
            done

            echo "🎉 Deployment successful!"